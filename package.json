{
  "name": "samuel-charry-portfolio",
  "version": "1.0.0",
  "private": true,
  "description": "Portafolio estático de Samuel Charry. HTML, CSS y JavaScript sin dependencias.",
  "scripts": {
    "start": "node scripts/serve.mjs",
    "check": "node --check js/content.js && node --check js/main.js && node --check scripts/serve.mjs"
  }
}
